# TTS 牌背源图

- `hand.png`：手牌牌背。
- `character.png`：角色牌牌背。

`npm run export:tts` 会将源图按比例居中缩放到 `750×1050`，缺少的边缘使用深色填充，不裁切、不拉伸原始花纹。

导出文件：

- `exports/tts/backs/back_hand.png`
- `exports/tts/backs/back_character.png`
