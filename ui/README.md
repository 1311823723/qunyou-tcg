# UI

当前 UI 尚未实现。

## 视觉方向

详见 `docs/style-guide.md`。

核心方向：暗色桌面、TCG 卡牌感、群友整活、清楚易读、轻微霓虹。

禁止廉价页游风和后台管理系统风。

## 未来 UI 开发约定

未来 UI 应该：

- 读取 `data/cards/*.json`
- 读取 `data/decks/*.json`
- 不要硬编码卡牌文本
- 卡牌展示必须像"收藏册里的卡"，不能是表格
- 标签使用小胶囊样式，主定位标签更突出

## 优先实现顺序

1. 卡牌列表页
2. 预组展示页
3. 1v1 桌面原型
4. 简单对局状态管理
