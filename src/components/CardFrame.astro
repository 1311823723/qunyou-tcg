---
export interface Props { class?: string; role?: string; }
const { class: extraClass = "", role } = Astro.props;
---
<div class={`card-frame ${extraClass}`} {...(role ? { "data-role": role } : {})}>
  <slot />
</div>
